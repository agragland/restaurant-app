import React from 'react';

import { CL_COLUMNS } from '../Columns';

import "./ChangeLog.css";

export default function ChangeLog(){
    return(
        <div className="ChangeLog">
            <h1>Change Log</h1>
            <div className="logDisplay">
                {/* Get data to dislay */}
                {/* Define the columns for the table */}
                {/* Use the data and comuns defined to create a table instance using react-table */}
                {/* Define a basic table structure using plain HTML */}
                {/* Use the table instance created to bring life to the HTML */}
                
            </div>
        </div>
    );
}